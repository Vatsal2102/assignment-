Conceputal Model

Designing the model by using https://app.gleek.io/ 
 
 ------------------------------------------------------------ 

// Create Entity for twincities

City
	int cityID
	string name
	string description
	string history
	decimal latitude
	decimal longitude
	string timezone
	string language
	int population
	int currencyID
	int countryID

// Entiy for country 
country
	int countryID
	string name
	int code


// Entity for the most used currency of the city
currency
	int currencyID
	string currency
	string symbol


// Entity for the ethnicity of the people living in the city
ethnicity
	int ethnicityID
	string ethnicity

// Junction table for city and ethnicity
city_ethnicity
	int cityID
	int ethnicityID

// Entity for the news of the city
news
	int newsID
	string title
	string content
	Date date
	int cityID

// Entity for city photos
photo
	int photoID
	string title
	blob image
	Date date
	int cityID

// Entity for relation bet news and photo
news_photo
	int newsID
	int photoID

// Entity for user
user
	int userID
	string name

// Entity for user commments
comment
	int commentID
	string content
	Date date
	int cityID
	int userID

// Entity for the some specific location/place of the city
place
	int placeID
	string name
	string description
	decimal latitude
	decimal longitude
	int cityID
	int categoryID

// Entity for the category of some location such as venue 
category
	int categoryID
	string name

// Entity for relation bet place and category
place_category
	int placeID
	int categoryID


//----------------------------------------------------//

// Relationships of the entities

City{1}--{1} country

// city can have many ethnicities and many enthnicites can belong to many cities
City {0..n}--{0..n} city_ethnicity
ethnicity {0..n}--{0..n} city_ethnicity

// city can have only one currency and currency can be involved in many citites
City {1}--{1..n}currency

// city will have none or many news and news can belong to many cities
City {1}--{0..n}news

// city can have many photos and photos can belong to many cities
City {1}--{0..n}photo

// none or more comments for the one city 
City {1}--{0..n}comment

// cities have many places but place will be specified to one city
City {1}--{0..n}place

// user and comment relation
user {1}--{0..n}comment

// A place can belong to multiple categories and a single category will have one or more places
place {1..n}--{1..n} place_category
category {1..n}--{1..n} place_category

// A single piece of news can have multiple associated photos.
// A single photo can be associated with multiple news items.
news {1..n}--{1..n} news_photo
photo {1..n}--{1..n} news_photo
